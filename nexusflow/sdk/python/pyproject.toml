[project]
name = "nexusflow-sdk"
version = "0.1.0"
description = "Python SDK for NexusFlow topology JSON / sysfs helpers"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7"]
